@model IEnumerable<latihanLogSig9.Models.Produk>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaProduk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Keterangan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaProduk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Keterangan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProdukID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProdukID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProdukID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
